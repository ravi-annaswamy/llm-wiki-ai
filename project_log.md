# Project Log — llm-wiki-ai

Top-level experience log. Detailed ingest/lint history lives in `wiki/log.md`.

## 2026-04-05 10:30 — ninth ingest (Verge on human-made content labelling)
Ingested `raw/2026-04-05-theverge-Really, you made this without AI Prove it.md` — Jess Weatherbed's April 2026 opinion piece surveying the fragmented landscape of human-made content labelling schemes (12+ competing entrants) against the backdrop of the "wholly ineffectual" industry-backed C2PA standard. First wiki source focused on **content provenance / authenticity** as an independent axis from quality, economics, or capability. Single-pass ingest (~2k words, fits under the 3k dense-source threshold). Yield: 1 source + 4 entities (c2pa, proudly-human, not-by-ai, proof-i-did-it) + 2 concepts (human-made-content-labelling hub, fingerprinting-real-vs-fake) + updates to 4 existing pages (coral-hart, pangram, ai-in-creative-writing, ai-novel-factory). Wiki now at 79 pages / 9 sources / 2 analyses.

**Main strategic moves:** (1) The Verge's core structural argument — Mosseri's "fingerprint real media, not fake media" — promotes **incentive alignment** to a first-class wiki concept, and ties it cleanly to the existing [[analyses/own-your-substrate]] analysis: both ask "which party is motivated to do the cooperative work this system depends on?" Proof I Did It's on-chain identity token is a creator-level own-your-substrate instance for trust infrastructure. (2) The source gives the wiki its first view into the **producer-side resistance** to labelling regimes — previously the wiki had the Shy Girl cancellation (reader-side unmasking) and Coral Hart's inconsistent disclosure (operator-side non-cooperation), but not the explicit economic argument for why labelling the AI side will always fail. That argument now lives at `concepts/fingerprinting-real-vs-fake.md`.

**Cross-source finding (important):** The Verge and the previously-ingested New Yorker essay give **different renderings of Coral Hart's Amazon disclosure practice**, both citing the same Alexandra Alter NYT article. Verge: "doesn't have a label on any of her books." New Yorker: "sometimes does and sometimes doesn't." Without the primary Alter piece in `raw/`, the wiki can't resolve this — flagged as an explicit cross-source discrepancy on `entities/coral-hart.md`, with the Verge's stronger-stated version noted as conservatively preferred (harder to reach by paraphrase error from a "sometimes" source). This is the wiki's first documented cross-source discrepancy; worth keeping an eye on as the corpus grows.

**Entity-creation discipline:** Resisted creating entity pages for the four quoted academics (Jonathan Stray, Nina Beguš, Thomas Beyer, Trevor Woods) — each provides one useful quote but no substantive discussion, which is the schema's non-creation threshold. Their quotes live on the source page and hub concept. Also resisted pages for *Made by Human*, *No-AI-Icon*, and the Authors Guild Human Authored Certification since the source only name-drops them; they're mentioned inline on the hub concept page instead. This holds the wiki's 12-scheme landscape to 4 entity pages, which is the right density given the source's depth on each.

**Ingest-time lens checks per CLAUDE.md:** The source does not match the producer-filter, task-shape, or scaffolding lenses (these target agentic-work/automated-research sources; this is a provenance/authenticity piece). Documented the non-match implicitly by not forcing a fit.

### My prompt
> ingest raw/2026-04-05-theverge-Really, you made this without AI Prove it

## 2026-04-04 23:55 — seventh ingest (score-based generative models, foundational ML theory)
Ingested `raw/2026-04-04-yang-song.net-Generative Modeling by Estimating Gradients of the Data Distribution.md` — Yang Song's widely cited ~7,500-word tutorial blog post compiling his 2019–2022 paper sequence on score-based generative modeling, culminating in the ICLR 2021 SDE framework that unified score-based models with denoising diffusion. First foundational-ML-theory source in the wiki (distinct from the applied / agentic / creative clusters). Dense source, so followed the two-pass ingest protocol. Yield: 1 source + 2 entities (yang-song, stefano-ermon) + 5 concepts (score-based-generative-models as the hub, score-matching, langevin-dynamics, diffusion-models, probability-flow-ode) + cross-link edits into three existing applied visual-AI concept pages (ai-filmmaking-india, ai-dubbing, hindu-mythology-ai-genre). Added a new "Generative model theory (foundational)" category in index.md. Wiki now at 67 pages / 7 sources / 2 analyses.

Main strategic move: this ingest makes the **substrate layer** beneath the wiki's applied visual-AI stories explicit. Indian studios, AI dubbing shops, AI novel factories, and the diffusion-based systems they all rent from Google, Microsoft, and open-source checkpoints are all downstream of Song / Ermon's 2019–2021 work. The cross-links on the applied pages now name the underlying model family, and the [[analyses/own-your-substrate]] thesis becomes concrete: operators rent the diffusion-model layer and own the domain pipelines / catalogs / distribution above it. Previously this split was argued at the analysis level but the "rented" substrate was unnamed in the wiki — now it has its own concept cluster.

Ingest-time lens checks per CLAUDE.md: the source does *not* match any of the producer-filter / task-shape / scaffolding lenses, because those lenses are specifically for agentic-work / automated-research sources and this is classical ML theory (gradient descent on Fisher divergence, no agent). Documented the non-match explicitly on the source page so the check isn't silently skipped.

## 2026-04-04 23:40 — schema improvements applied to CLAUDE.md
Applied all three schema proposals from the refactor pass to `CLAUDE.md`. (1) The REFACTOR workflow now has an explicit "analysis-page graduation" step: themes with ≥3 instances across sources get promoted to their own analysis page. (2) The Log Format spec now has an optional "Failed approaches / dead ends" line, mirroring the `CHANGELOG.md` pattern the wiki catalogues — the wiki now practices the pattern it documents. (3) The Domain Configuration section has a new "Ingest-time lenses for agentic-work / automated-research sources" subsection with three standing checks (producer–filter architecture, task shape → agent topology, scaffolding pieces) to speed up future integration of sources in this topical area. All three changes are meta-improvements: they turn insights from the last two ingests into standing project conventions.

## 2026-04-04 23:25 — refactor pass
Ran a refactor pass. No pages were too large (largest is log.md at 119 lines). A handful of entity stubs are under ~100 words but all are legitimate anchor references and were left alone. The substantive move was **promoting the "LLM as producer with filter loop" theme to its own standalone analysis page**: `analyses/producer-filter-pattern.md`. The theme had four clean instances across the wiki (Karpathy knowledge bases, AlphaEvolve algorithm discovery, Coral Hart novel factory, CLAX Boltzmann solver) plus a fifth implied instance (Coefficient Bio drug discovery) — well past the schema's "3+ instances = dedicated analysis page" threshold, and the last two log entries had explicitly flagged it as overdue. Extracted the unified analysis out of `concepts/test-oracle-for-agents.md` (trimmed to a brief summary + outbound link), added cross-link from the sibling `analyses/own-your-substrate.md`, and updated overview.md to mark the theme as graduated. Also prepared a short set of schema improvement proposals for CLAUDE.md (see the turn's refactor report); did not apply them yet, pending user review. Wiki now at 59 pages / 6 sources / 2 analyses.

## 2026-04-04 23:05 — sixth ingest (long-running agentic coding for science)
Ingested `raw/long-running-claude-for-scientific-computing.md` — Anthropic post by Siddharth Mishra-Sharma (Discovery team) on multi-day autonomous Claude Code workflows applied to scientific computing, with CLAX (differentiable JAX Boltzmann solver vs CLASS reference) as the worked example. ~1.7k words, single-pass ingest. 11 new pages: 1 source, 4 entities (siddharth-mishra-sharma, clax-project, class-boltzmann-solver, anthropic-c-compiler-project), 6 concepts (long-running-agentic-coding, test-oracle-for-agents, agent-persistent-memory, ralph-loop, agentic-laziness, agent-driven-scientific-computing). Also updated the anthropic entity page to add the Discovery team + long-horizon agentic coding thread alongside its existing life-sciences thread. Wiki now at 58 pages / 6 sources / 1 analysis.

Three cross-wiki observations worth flagging: (1) the **"LLM as active producer with filter loop"** theme now has a fourth clean instance (CLAX joining Karpathy KB, AlphaEvolve, Coral Hart) — this is well past the threshold where it deserves its own standalone analysis page, currently tracked implicitly inside `concepts/test-oracle-for-agents.md`. (2) New generalizable observation: **task shape determines agent topology** — the same Anthropic methodology manifested as a 2,000-session parallel swarm for the C compiler project and a single sequential agent for CLAX, because a deeply coupled numerical pipeline requires causal tracing. (3) The `CLAUDE.md` + `CHANGELOG.md` pair is structurally identical to this wiki's own `CLAUDE.md` + `wiki/log.md` split — both are file-over-app "agent context pairs" where portable plan + append-mostly progress log together substitute for hidden vector-store memory.

## 2026-04-04 20:15 — first ingest
Ingested the single markdown file in `raw/assets/` (TechCrunch article on Anthropic acquiring Coefficient Bio for $400M). Short source, single-pass workflow per CLAUDE.md. Produced 1 source page, 6 entity pages, 1 concept page; updated index and overview. Wiki now has 8 pages total from 1 source. See `wiki/log.md` for the file-by-file breakdown.

Note: the only raw file lives at `raw/assets/` rather than `raw/` directly — future ingests should check both locations.

## 2026-04-04 22:30 — fifth ingest (creative writing / AI fiction)
Ingested `raw/newyorker-is-it-wrong-write-a-book-with-ai.md` — New Yorker essay framing the AI-and-fiction debate around the Shy Girl Hachette cancellation, Coral Hart's AI novel factory, the Roland TR-808 drum machine analogy, and a layered-authorship argument. New domain for the wiki (first creative-writing / publishing source). 8 new pages: 1 source, 3 entities (shy-girl-ai-novel, coral-hart, pangram), 4 concepts (ai-in-creative-writing hub, layered-authorship, ai-novel-factory, drum-machine-analogy-for-ai). Wiki now at 47 pages / 5 sources / 1 analysis.

Two themes crossed their instance thresholds with this ingest: (1) the **reception paradox** now has three instances (Mahabharat, Raanjhanaa, Shy Girl) across two media and two cultural contexts, and (2) the **LLM as active producer of structured artifacts** theme now has three instances (Karpathy KB, AlphaEvolve, Coral Hart) — both deserve dedicated analysis pages in a future pass. Also surfaced a new cross-cutting lens, **layered authorship**, that retroactively clarifies the earlier ai-film-re-editing and ai-dubbing concept pages as instances of layer-specific (rather than wholesale) AI intervention.

## 2026-04-04 21:55 — lint pass
Ran a full health check on the wiki. Clean bill overall (no orphans, no low-inbound pages, no contradictions). Applied low-friction fixes only: corrected the page-count drift in index.md (38 → 39), fixed a stale `raw/assets/` path in the Anthropic source frontmatter (the file was moved to `raw/` directly at some point), and retroactively added prompt sections to the two older source pages that were written before that global-instruction convention was consistently applied. Deferred the higher-effort new-page batch (Galleri5, SAG-AFTRA, VAD-CFR, SHOR-PSRO, Mahabharat AI series, Raanjhanaa re-release, Gemini 2.5 Pro, OpenSpiel, Yash Raj Films, DGA) — all flagged as mentioned on 3+ pages but not yet promoted to their own entries. Full findings in the conversation turn before this log entry.

## 2026-04-04 21:30 — fourth ingest (game theory / automated research)
Ingested `raw/deepmind-gametheory-algo-auto-research.md.md` — MarkTechPost writeup of a DeepMind paper on AlphaEvolve applied to MARL in imperfect-information games. New domain for the wiki (first game theory content). 7 new pages: 1 source, 2 entities (google-deepmind, alphaevolve), 4 concepts (llm-driven-algorithm-discovery, marl-imperfect-information-games, counterfactual-regret-minimization, policy-space-response-oracles). Wiki now at 38 pages / 4 sources / 1 analysis.

Emerging cross-cutting theme captured in overview.md: **LLM as active producer of structured artifacts, not query-answerer.** Both the Karpathy pattern (LLM authors wiki pages, linting as eval loop) and AlphaEvolve (LLM authors algorithm code, exploitability as eval loop) are instances of this. Candidate for a future analysis page once a third instance shows up.

Note on raw filename: the file has a double `.md.md` extension — future ingests should accept this and not rename (keeping raw/ immutable per convention).

## 2026-04-04 21:10 — first analysis page
Created `wiki/analyses/own-your-substrate.md` — the wiki's first analysis page (first use of the `analyses/` folder). Synthesizes the cross-cutting pattern across all three ingested sources: vertical AI operators own the substrate layers that compound (domain talent, pipelines, structured knowledge) and rent the commodity ones (frontier model APIs, cloud compute). Includes a counter-read section (where the pattern doesn't apply) and an explicit meta note that this wiki is itself an instance of the pattern it describes. Wiki now at 31 pages / 3 sources / 1 analysis.

## 2026-04-04 20:55 — third ingest (meta)
Ingested `raw/karpathy-llm-wiki-idea.md` — VentureBeat writeup of Karpathy's "LLM Knowledge Bases" pattern. This is the pattern the project itself implements, so the source is unusually meta: it's simultaneously subject matter and the reference document for the wiki's own architecture. Produced 1 source + 3 entity + 7 concept pages (11 new pages). Added a new top-level category "Knowledge management methodology" in index.md. Overview.md now frames the wiki as both a research workspace and a live implementation of the Karpathy pattern. Wiki now has 3 sources and ~30 pages.

Next potential step: consider a `wiki-staging/` directory to implement the contamination-mitigation pattern before running larger ingest batches, and link `CLAUDE.md` to `concepts/llm-knowledge-bases.md` as the canonical reference.

## 2026-04-04 20:35 — second ingest
Ingested `raw/ai-indian-films.md` (Reuters feature on AI in Indian film). Dense but under single-pass threshold; produced 1 source page, 7 entity pages, 4 concept pages (12 new pages). Wiki now has 2 sources and ~19 pages total. Overview updated with a second vertical-AI story (after the Anthropic/Coefficient Bio life-sciences thread). A cross-cutting theme has started to emerge: regulatory and contractual environments determine where frontier AI practices can deploy at scale. See `wiki/log.md` for the file-by-file breakdown.
